num=int(input('enter the number'))
reversenum=0
temp=num
while temp>0:
  digit=temp%10
  reversenum=reversenum*10+digit
  temp=temp//10
if num==reversenum:
  print(f'{num} is palindrome numbers')
else:
  print(f'{num} is not palindrome numbers')
